openapi: 3.0.3
info:
  title: Minimal API
  version: 1.0.0
paths:
  /items:
    get:
      responses:
        "200":
          description: OK
    post:
      responses:
        "201":
          description: Created
  /items/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
      responses:
        "200":
          description: OK
